1. application.yml  프로티값 설정
spring: 
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        '[format_sql]': true

2. controller ----- service ----- repository 인터페이스(dao)  
            <- dto ->    <- entity->   

3. Entity

